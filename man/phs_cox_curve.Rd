% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phs_cox_curve.R
\name{phs_cox_curve}
\alias{phs_cox_curve}
\title{Cox model survival curves at specified PHS percentiles}
\usage{
phs_cox_curve(
  data,
  phs = "phs",
  time = "age",
  event = "status",
  percentiles = c(0.01, 0.05, 0.2, 0.5, 0.8, 0.95, 0.99),
  ref_data = NULL,
  output = "plot",
  conf_int = TRUE,
  conf_int_alpha = 0.15,
  palette = "hazrd",
  ...
)
}
\arguments{
\item{data}{data.frame with columns specified by \code{phs}, \code{time}, \code{event}}

\item{phs}{string or numeric vector; column name or vector of PHS values}

\item{time}{string or numeric vector; column name or vector of event times}

\item{event}{string or numeric vector; column name or vector of event
indicators (0/1)}

\item{percentiles}{numeric vector of percentiles strictly in (0, 1) at
which to compute Cox-predicted survival curves; default
\code{c(0.01, 0.05, 0.20, 0.50, 0.80, 0.95, 0.99)}}

\item{ref_data}{optional data.frame used to compute the PHS value at each
requested percentile (training reference); if \code{NULL}, percentiles are
computed from \code{data}}

\item{output}{\code{'plot'} (default) or \code{'data'}}

\item{conf_int}{logical; include confidence intervals in output/plot}

\item{conf_int_alpha}{numeric; alpha for confidence ribbons when plotting}

\item{palette}{string; colour palette for plots (default: \code{'hazrd'})}

\item{...}{additional args (reserved)}
}
\value{
ggplot object (when \code{output = 'plot'}) or data.frame (when
\code{output = 'data'}). The data frame has columns \code{time}, \code{estimate},
\code{conf.low}, \code{conf.high}, \code{percentile}, and \code{percentile_value}.
}
\description{
Fit a Cox proportional-hazards model with \code{phs} as the sole predictor and
return predicted survival curves for individuals at specified PHS
percentiles. Unlike \code{\link[=phs_km_curve]{phs_km_curve()}}, these are smooth model-based
predictions rather than empirical group estimates.
}
\details{
Percentile values are computed from \code{ref_data} when supplied, which
supports a training→validation workflow where the reference distribution
comes from a training cohort.
}
