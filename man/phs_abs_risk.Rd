% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phs_abs_risk.R
\name{phs_abs_risk}
\alias{phs_abs_risk}
\title{Cumulative incidence (absolute risk) curves stratified by PHS percentile}
\usage{
phs_abs_risk(
  data,
  phs = "phs",
  time = "age",
  event = "status",
  competing_event = NULL,
  intervals = list(c(0.95, 1), c(0.8, 1), c(0.3, 0.7), c(0, 0.2)),
  ref_data = NULL,
  method = "km",
  time_points = NULL,
  output = "plot",
  conf_int = TRUE,
  conf_int_alpha = 0.15,
  palette = "hazrd",
  ...
)
}
\arguments{
\item{data}{data.frame with columns specified by \code{phs}, \code{time}, and \code{event}}

\item{phs}{string; column name of the continuous PHS values}

\item{time}{string; column name of the time-to-event variable}

\item{event}{string; column name of the event indicator (0 = censored,
1 = event of interest)}

\item{competing_event}{string or NULL; column name of the competing event
indicator (0/1). Required when \code{method = "aalen_johansen"}.}

\item{intervals}{list of \code{c(lo, hi)} pairs defining percentile bands, e.g.
\code{list(c(0.80, 1), c(0, 0.20))}. Bands may overlap. Default produces four
bands matching \code{phs_km_curve()}: top 5 (0.95-1), top 20 (0.80-1),
middle 40 (0.30-0.70), and bottom 20 (0-0.20).}

\item{ref_data}{optional data.frame used as the reference population for
computing percentile cutpoints (e.g., a training cohort).}

\item{method}{string; estimation method. \code{"km"} (default): cumulative
incidence as 1 minus the Kaplan-Meier survival estimate. Appropriate
when censoring is independent and no competing risks are present.
\code{"aalen_johansen"}: Aalen-Johansen cumulative incidence estimator;
recommended when competing risks are present (requires
\code{competing_event}). \code{"fine_gray"}: stub, not yet implemented.}

\item{time_points}{numeric vector or NULL; when \code{output = "data"}, return
estimates interpolated to these specific time points (one row per
stratum x time_point combination). When NULL, all event-time rows are
returned.}

\item{output}{string; \code{"plot"} (default) returns a ggplot object,
\code{"data"} returns a tidy data frame.}

\item{conf_int}{logical; whether to include confidence intervals
(default TRUE).}

\item{conf_int_alpha}{numeric; transparency for CI ribbons when plotting
(default 0.15).}

\item{palette}{string; color palette name (default \code{"hazrd"}).}

\item{...}{additional arguments (reserved for future use).}
}
\value{
A ggplot object when \code{output = "plot"}, or a tidy data frame when
\code{output = "data"}. The data frame has columns \code{time}, \code{stratum},
\code{risk_estimate}, \code{conf.low}, \code{conf.high}, \code{n.risk}, \code{n.event}. When
\code{time_points} is supplied with \code{output = "data"}, one row per stratum x
time_point is returned.
}
\description{
Computes cumulative incidence curves for PHS percentile-stratified groups
using 1 - Kaplan-Meier (method = "km") or the Aalen-Johansen estimator
when competing risks are present (method = "aalen_johansen"). Returns a
ggplot2 object or a tidy data frame.
}
\examples{
data(test_data)

# Default: cumulative incidence plot (top 5, top 20, middle 40, bottom 20)
phs_abs_risk(test_data)

# Return tidy data for custom plotting
abs_data <- phs_abs_risk(test_data, output = "data")

# Extract estimates at specific time points
phs_abs_risk(test_data, time_points = c(60, 70, 80), output = "data")

# Custom intervals
phs_abs_risk(test_data,
  intervals = list(c(0.80, 1), c(0, 0.20)))

}
