% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phs_km_curve.R
\name{phs_km_curve}
\alias{phs_km_curve}
\title{Kaplan-Meier curves stratified by PHS percentile}
\usage{
phs_km_curve(
  data,
  phs = "phs",
  time = "age",
  event = "status",
  intervals = list(c(0.95, 1), c(0.8, 1), c(0.3, 0.7), c(0, 0.2)),
  breaks = NULL,
  ref_data = NULL,
  output = "plot",
  conf_int = TRUE,
  conf_int_alpha = 0.15,
  palette = "hazrd",
  risk_table = FALSE,
  ...
)
}
\arguments{
\item{data}{data.frame with columns specified by \code{phs}, \code{time}, \code{event}}

\item{phs}{string or numeric vector; column name or vector of PHS values}

\item{time}{string or numeric vector; column name or vector of event time}

\item{event}{string or numeric vector; column name or vector of event indicator (0/1)}

\item{intervals}{list of \code{c(lo, hi)} pairs defining percentile bands, e.g.
\code{list(c(0.80, 1), c(0, 0.20))}. Bands may overlap. Takes precedence over
\code{breaks} when non-\code{NULL}. Default produces four bands:
top 5 (0.95-1), top 20 (0.80-1), middle 40 (0.30-0.70), and bottom 20 (0-0.20).}

\item{breaks}{numeric vector of percentile cutpoints strictly in (0, 1)
used to form exclusive bands (legacy); ignored when \code{intervals} is non-\code{NULL}.
Retained for backward compatibility.}

\item{ref_data}{optional data.frame used to compute percentile cutpoints (training reference)}

\item{output}{'plot' (default) or 'data'}

\item{conf_int}{logical; include confidence intervals in output/plot}

\item{conf_int_alpha}{numeric; alpha for confidence ribbons when plotting}

\item{palette}{string; name of color palette to use for plots (default: 'hazrd')}

\item{risk_table}{logical; if TRUE returns numbers-at-risk in the data output (experimental)}

\item{...}{additional args (reserved)}
}
\value{
ggplot object (when output = 'plot') or data.frame (when output = 'data')
}
\description{
Compute Kaplan-Meier survival curves stratified by PHS percentile
groups. Returns either a ggplot object (output = "plot") or a tidy
data frame (output = "data"). Preset strata and custom cutpoints are
supported; percentiles are computed from \code{ref_data} when supplied so a
training->validation workflow is possible.
}
