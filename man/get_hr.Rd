% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{get_hr}
\alias{get_hr}
\title{Deprecated: use phs_metrics() instead}
\usage{
get_hr(
  data = NULL,
  phs = "phs",
  age = "age",
  status = "status",
  lower_interval = 0.2,
  upper_interval = 0.8,
  CI = FALSE,
  bootstrap_iterations = 1000,
  swc = FALSE,
  swc_popnumcases = NULL,
  swc_popnumcontrols = NULL
)
}
\arguments{
\item{data}{a data.frame containing PHS data}

\item{phs}{column name for PHS values. Default \code{"phs"}}

\item{age}{column name for time-to-event. Default \code{"age"}}

\item{status}{column name for event indicator. Default \code{"status"}}

\item{lower_interval}{single quantile (upper bound of denominator band,
lower bound defaults to 0) or length-2 vector \code{c(lower, upper)}.
Default \code{0.20}}

\item{upper_interval}{single quantile (lower bound of numerator band,
upper bound defaults to 1) or length-2 vector \code{c(lower, upper)}.
Default \code{0.80}}

\item{CI}{logical. If \code{TRUE}, bootstrap CIs are computed. Default \code{FALSE}}

\item{bootstrap_iterations}{number of bootstrap replicates. Default \code{1000}}

\item{swc}{logical. Sample weight correction. Not supported in
\code{phs_metrics()} — a warning is emitted if \code{TRUE}. Default \code{FALSE}}

\item{swc_popnumcases}{ignored (swc not yet implemented in \code{phs_metrics()})}

\item{swc_popnumcontrols}{ignored (swc not yet implemented in \code{phs_metrics()})}
}
\value{
A list with elements \code{index}, \code{value}, \code{conf.low}, \code{conf.high},
and \code{iters}, matching the return structure of the previous release.
}
\description{
\code{get_hr()} is deprecated as of hazrd 0.2.0. Please use
\code{\link[=phs_metrics]{phs_metrics()}} with \code{metrics = "HR"} instead.
}
\seealso{
\code{\link[=phs_metrics]{phs_metrics()}}
}
