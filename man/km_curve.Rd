% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{km_curve}
\alias{km_curve}
\title{Deprecated: use phs_km() instead}
\usage{
km_curve(
  data = NULL,
  phs = "phs",
  age = "age",
  status = "status",
  interval = c(0, 1),
  age_range = 40:100,
  scale = FALSE,
  inverse = FALSE
)
}
\arguments{
\item{data}{a data.frame containing PHS data}

\item{phs}{column name for PHS values. Default \code{"phs"}}

\item{age}{column name for time-to-event. Default \code{"age"}}

\item{status}{column name for event indicator. Default \code{"status"}}

\item{interval}{numeric vector of length 2 specifying the lower and upper
quantiles for filtering subjects. Default \code{c(0, 1)}. Has no direct
equivalent in \code{phs_km()} — a warning is emitted and the argument is
ignored; all subjects are included in the stratified output.}

\item{age_range}{ages over which to calculate curves. Default \code{40:100}.
Not supported in \code{phs_km()} — a warning is emitted and the argument
is ignored.}

\item{scale}{logical. If \code{TRUE}, PHS is centred and scaled to unit
variance. Not supported in \code{phs_km()} — a warning is emitted and the
argument is ignored. Scale your PHS column before calling \code{phs_km()}.}

\item{inverse}{logical. If \code{TRUE}, PHS is multiplied by \code{-1}. Not
supported in \code{phs_km()} — a warning is emitted and the argument is
ignored. Multiply your PHS column manually before calling \code{phs_km()}.}
}
\value{
A data.frame as returned by \code{\link[=phs_km_curve]{phs_km_curve()}} with \code{output = "data"}.
}
\description{
\code{km_curve()} is deprecated as of hazrd 0.2.0. Please use
\code{\link[=phs_km_curve]{phs_km_curve()}} instead.
}
\seealso{
\code{\link[=phs_km_curve]{phs_km_curve()}}
}
