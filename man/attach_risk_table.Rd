% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phs_risk_table.R
\name{attach_risk_table}
\alias{attach_risk_table}
\title{Attach a numbers-at-risk table below a curve plot}
\usage{
attach_risk_table(plot, risk_table, heights = c(3, 1), ...)
}
\arguments{
\item{plot}{ggplot object returned by \code{\link[=phs_abs_risk]{phs_abs_risk()}} or \code{\link[=phs_km_curve]{phs_km_curve()}},
optionally with additional layers applied.}

\item{risk_table}{ggplot object returned by \code{\link[=phs_risk_table]{phs_risk_table()}}, or a
data.frame. When a data.frame is supplied, \code{\link[=phs_risk_table]{phs_risk_table()}} is called
internally; any additional arguments (e.g. \code{intervals}, \code{time_points},
\code{palette}) are forwarded via \code{...}.}

\item{heights}{numeric vector of length 2 controlling the relative heights
of the curve and table panels (default \code{c(3, 1)}).}

\item{...}{additional arguments forwarded to \code{\link[=phs_risk_table]{phs_risk_table()}} when
\code{risk_table} is a data.frame. Ignored when \code{risk_table} is already a
ggplot.}
}
\value{
A patchwork object combining the curve and risk table.
}
\description{
Combines a \code{\link[=phs_abs_risk]{phs_abs_risk()}} or \code{\link[=phs_km_curve]{phs_km_curve()}} plot with a numbers-at-risk
strip using patchwork. Accepts either an already-built risk table ggplot
(from \code{\link[=phs_risk_table]{phs_risk_table()}}) or a raw data.frame, in which case
\code{\link[=phs_risk_table]{phs_risk_table()}} is called internally with any extra arguments passed via
\code{...}.
}
\examples{
data(test_data)

# Quick usage: build and attach in two lines
p <- phs_abs_risk(test_data)
attach_risk_table(p, test_data)

# Full control: customise both objects before combining
p  <- phs_abs_risk(test_data) + ggplot2::theme_classic()
rt <- phs_risk_table(test_data, time_points = c(55, 65, 75, 85))
attach_risk_table(p, rt)

}
