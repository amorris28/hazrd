% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phs_risk_table.R
\name{phs_risk_table}
\alias{phs_risk_table}
\title{Numbers-at-risk table for PHS percentile-stratified groups}
\usage{
phs_risk_table(
  data,
  phs = "phs",
  time = "age",
  event = "status",
  competing_event = NULL,
  intervals = list(c(0.95, 1), c(0.8, 1), c(0.3, 0.7), c(0, 0.2)),
  ref_data = NULL,
  method = "km",
  palette = "hazrd",
  time_points = NULL,
  ...
)
}
\arguments{
\item{data}{data.frame with columns specified by \code{phs}, \code{time}, and \code{event}}

\item{phs}{string; column name of the continuous PHS values}

\item{time}{string; column name of the time-to-event variable}

\item{event}{string; column name of the event indicator (0 = censored,
1 = event of interest)}

\item{competing_event}{string or NULL; column name of the competing event
indicator. Required when \code{method = "aalen_johansen"}.}

\item{intervals}{list of \code{c(lo, hi)} pairs defining percentile bands.
Should match the \code{intervals} used in the accompanying
\code{\link[=phs_abs_risk]{phs_abs_risk()}} or \code{\link[=phs_km_curve]{phs_km_curve()}} call.}

\item{ref_data}{optional data.frame used as the reference population for
computing percentile cutpoints.}

\item{method}{string; \code{"km"} (default) or \code{"aalen_johansen"}. Should
match the method used in the accompanying curve function.}

\item{palette}{string; color palette name. Should match the palette used
in the accompanying curve plot (default \code{"hazrd"}).}

\item{time_points}{numeric vector or NULL; time values at which to display
numbers at risk. For example, \code{c(50, 60, 70, 80, 90)}. When NULL
(default), values are chosen automatically using \code{pretty()}.}

\item{...}{additional arguments (reserved for future use).}
}
\value{
A ggplot2 object displaying the numbers at risk over time for each
stratum. Combine with the output of \code{\link[=phs_abs_risk]{phs_abs_risk()}} or
\code{\link[=phs_km_curve]{phs_km_curve()}} using patchwork, e.g.:
\code{p / phs_risk_table(data) + patchwork::plot_layout(heights = c(3, 1))}.
}
\description{
Builds a ggplot2 numbers-at-risk strip intended for combining with a
\code{\link[=phs_abs_risk]{phs_abs_risk()}} or \code{\link[=phs_km_curve]{phs_km_curve()}} plot using patchwork. Accepts the
same data and interval arguments as those functions and computes the
survival data internally.
}
\examples{
data(test_data)

p  <- phs_abs_risk(test_data)
rt <- phs_risk_table(test_data)

# Specify exact ages for the at-risk columns:
rt <- phs_risk_table(test_data, time_points = c(50, 60, 70, 80))

# Combine with patchwork:
# p / rt + patchwork::plot_layout(heights = c(3, 1))

}
